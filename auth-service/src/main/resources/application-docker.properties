# Overrides for docker-compose: container-to-container hostname instead of
# the host-facing localhost port used by mvnw spring-boot:run.
spring.datasource.url=jdbc:postgresql://postgres:5432/sarb?currentSchema=auth
