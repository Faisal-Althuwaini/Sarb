# Overrides for docker-compose: container-to-container hostnames instead of
# the host-facing localhost ports used by mvnw spring-boot:run.
spring.datasource.url=jdbc:postgresql://postgres:5432/sarb?currentSchema=telemetry
spring.kafka.bootstrap-servers=kafka:29092
